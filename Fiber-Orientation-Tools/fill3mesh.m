function fill3mesh(p, tricon, fval, varargin)%FILL3MESH(P, TRICON, FVAL) creates a "fill" plot of the nodal values%    of a scalar function on a 3-D mesh of triangles.  %    P (3 x numnode) gives the nodal coordinates, TRICON (numelt x 3)%    is the nodal connectivity for each element, and FVAL (numnode) is%    a vector of the function values at each node.  %%FILL3MESH(P, TRICON, FVAL, EDGALPH) sets the EdgeAlpha property of each%    triangle edge to EDGALPH.  Use EDGALPH = 0 to make the edge invisible.%%    See FILL3ELTS for a plot using element values.hold on% Figure out number of elements & nodes per element[nelts, ~] = size(tricon);% Plot the elementsfor i = 1:nelts    pn = p(:,tricon(i,:)); % Each column of pn gives x,y,z for a node    hp = fill3(pn(1,:), pn(2,:), pn(3,:), fval(tricon(i,:)));    if nargin >= 4        set(hp, 'EdgeAlpha', varargin{1})    end    if nargin >= 5        set(hp, 'LineWidth', varargin{2})    endend% Dress up the plotaxis equalview(3)set(gca, 'FontSize', 16)xlabel('x'); ylabel('y'); zlabel('z');box ongrid on%axis off